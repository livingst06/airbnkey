export type Apartment = {
    id: string
    slug: string
    title: string
    beds: number
    bathrooms: number
    advantages: string[]
    description: string
    latitude: number
    longitude: number
    images: string[]
  }