# Base SQLite (développement) — après clone : cp .env.example .env puis pnpm db:push && pnpm db:seed
DATABASE_URL="file:./prisma/dev.db"

# Optionnel : carte vectorielle MapTiler (toponymes, style pro). Compte gratuit sur maptiler.com
# NEXT_PUBLIC_MAPTILER_API_KEY=

# Admin CMS (déjà utilisé par l’app)
# NEXT_PUBLIC_ADMIN_MODE=true
